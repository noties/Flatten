apply plugin: 'java'

dependencies {
    
    compile 'com.google.code.gson:gson:2.4'

    testCompile 'junit:junit:4.12'
}

if (project.hasProperty('POM_NAME')) {
    apply from: '../gradle_maven_jar_publish.gradle'
}